A simple command line tool to encode and decodes files using huffman coding
*Usage*
To encode:
'''bash
huffman_encoding -e [inputPath] -o [outputPath]
''''
To decode:
'''bash
huffman_encoding -d [inputPath] -o [outputPath]
''''
